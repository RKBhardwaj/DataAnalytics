import * as common from './constants/common';
import * as navigation from './constants/navigation';
import * as dashboard from './constants/dashboard';
import * as visualization from './constants/visualization';
import * as routes from './constants/routes';

const constants = {
  common,
  navigation,
  dashboard,
  visualization,
  routes
};

export default constants;
