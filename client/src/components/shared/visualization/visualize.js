import React, { Component } from 'react';

const visualize = (props) => {
  const {
    type,
    data
  } = props;

  return (
    <div>
      Visualize chart
    </div>
  );     
}

export default visualize;