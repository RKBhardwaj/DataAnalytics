import React from 'react';

const LoginForm = (props) => {
  const {
    isAuthenticUser,
    loginAttempt
  } = props;

  return (
    <div>
      <h1>Login Form</h1>
    </div>
  );
};

export default LoginForm;
